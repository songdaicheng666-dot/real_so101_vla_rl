dataset:
  repo_id: local/so101_static_validation_v2
  root: artifacts/datasets/so101_static_validation_v2
  task: Pick up the blue cube and place it in T0.
  layout_id: competition_2026_basic_t0
  trial_id_prefix: static-validation-basic-t0
  num_episodes: 10
  episode_time_s: 8
  reset_time_s: 0
  fps: 30
  rgb_use_videos: true
  purpose: static_validation
  manual_confirmation: false

follower:
  port: /dev/serial/by-id/usb-1a86_USB_Single_Serial_5B79050068-if00
  id: my_follower_arm
  calibration_dir: null

leader:
  port: /dev/serial/by-id/usb-1a86_USB_Single_Serial_5B79049969-if00
  id: my_leader_arm
  calibration_dir: null

cameras:
  profile: ../hardware/cameras/so101_competition_2026.yaml

sync:
  tolerance_ms: 25
  timeout_ms: 200
  max_consecutive_failures: 3

max_relative_target: null
