# OpenVLA 与 OpenVLA-OFT：模型原理、异同与问答

本文整理了 OpenVLA 和 OpenVLA-OFT 的核心原理、模型结构、动作表示、训练与推理方式，以及它们用于真实机械臂时的差异。文末进一步解释两个容易混淆的问题：机械臂末端的“位置变化”和“姿态变化”分别是什么，以及原版 OpenVLA 和 OpenVLA-OFT 的输出头是否都属于 MLP。

## 1. 什么是 VLA

VLA 是 Vision-Language-Action，即视觉—语言—动作模型。它根据摄像头图像和自然语言指令，直接预测机器人可以执行的动作。

以“把红色方块放进碗里”为例，输入通常包括：

- 视觉：摄像头拍摄的机器人和操作场景。
- 语言：“把红色方块放进碗里。”
- 机器人本体状态，可选：关节角、夹爪状态等。

输出是机器人控制动作，而不是对场景的文字描述。最基本的形式为：

$$
a_t=\pi_\theta(I_t,l)
$$

其中：

- $I_t$ 是时刻 $t$ 的图像；
- $l$ 是语言指令；
- $a_t$ 是模型在时刻 $t$ 预测的动作。

当模型支持多相机、机器人状态和动作块时，可以写成：

$$
[a_t,a_{t+1},\ldots,a_{t+K-1}]
=
\pi_\theta(I_t^1,\ldots,I_t^M,l,s_t)
$$

其中 $M$ 是摄像头数量，$s_t$ 是机器人状态，$K$ 是一次预测的未来动作步数。

模型输出的动作还需要交给对应的底层控制器。例如，末端位姿增量需要笛卡尔空间控制器转换成各关节运动；关节位置目标则需要关节位置控制器执行。

## 2. OpenVLA

OpenVLA 是一个开源的通用机器人 VLA。它在 Prismatic-7B VLM 的基础上训练，主要由以下部分组成：

- SigLIP 和 DINOv2 组成的融合视觉编码器；
- 将视觉特征映射到语言模型嵌入空间的投影网络；
- Llama 2 7B 语言模型骨干；
- 将机器人动作表示成离散 token 的动作 tokenizer；
- 语言模型输出头。

OpenVLA 使用约 97 万条来自 Open X-Embodiment 数据集合的真实机器人轨迹进行训练，目标是从不同机器人、场景和任务中学习可迁移的视觉、语言和操作知识。

### 2.1 数据流

```text
摄像头图像
   ├──> DINOv2 ──┐
   └──> SigLIP ──┴──> 融合视觉特征 ──> 视觉投影器 ──┐
                                                         ├──> Llama 2
语言指令 ────────────────────────────────────────────────┘
                                                              ↓
                                                    逐个生成动作 token
                                                              ↓
                                                    反离散化和反归一化
                                                              ↓
                                                       当前一步动作
```

可以粗略地把 DINOv2 理解为提供较强的物体结构和空间视觉特征，把 SigLIP 理解为提供经过图文对齐训练的语义特征。二者没有严格的一一分工，但融合后能够同时帮助模型理解“看到了什么”和“语言指令指的是哪个目标”。

视觉特征通过投影网络转换到 Llama 使用的嵌入维度，再与语言 token 一起参与动作预测。模型不需要先输出一段场景描述，再把描述转换成动作。

### 2.2 动作表示

原版 OpenVLA 的典型动作有 7 个维度：

$$
a_t=
[\Delta x,\Delta y,\Delta z,
\Delta r_x,\Delta r_y,\Delta r_z,g]
$$

其中：

- $\Delta x,\Delta y,\Delta z$ 表示末端位置变化；
- $\Delta r_x,\Delta r_y,\Delta r_z$ 表示末端姿态变化；
- $g$ 表示夹爪动作。

这里的“7 维动作”不等于“机器人有 7 个关节”。它描述的是策略和底层控制器之间的动作接口。

OpenVLA 会把每个连续动作维度分别离散成 256 个区间。论文使用训练动作的第 1 和第 99 百分位确定各维度的离散范围，降低少量异常值对量化范围的影响。每个离散区间随后映射成语言模型词表中的一个 token。

例如：

```text
连续动作：
[0.012, -0.004, 0.008, 0.00, 0.02, -0.01, 1.0]

离散化后：
[第 132 档, 第 126 档, 第 130 档, ..., 夹爪档位]

再映射成：
[action_token_1, action_token_2, ..., action_token_7]
```

执行时再进行反向转换：

```text
动作 token → 离散区间代表值 → 反归一化 → 机器人动作
```

这种设计的优点是可以直接复用语言模型“预测下一个 token”的训练与推理机制。缺点是会引入量化误差，而且生成多个动作 token 会带来顺序解码延迟。

### 2.3 自回归动作生成

原版 OpenVLA 依次预测同一时刻的各个动作维度：

```text
x → y → z → rx → ry → rz → gripper
```

预测后一个动作 token 时，可以使用已经生成的前序动作 token：

$$
p(a_t\mid I_t,l)
=
\prod_{d=1}^{D}
p(a_t^{(d)}\mid I_t,l,a_t^{(<d)})
$$

这里的顺序生成首先发生在一个动作内部。它不表示模型每生成一个动作维度，机器人就立即执行一次运动。

训练使用动作 token 上的交叉熵损失：

$$
\mathcal L_{\mathrm{CE}}
=
-\sum_d\log p_\theta(z_t^{(d)}\mid I_t,l,z_t^{(<d)})
$$

训练时可以通过 teacher forcing 并行计算多个 token 位置的损失；实际推理时仍需要逐个生成 token。这个差异是理解原版 OpenVLA 推理速度的关键。

## 3. OpenVLA-OFT

OFT 全称为 Optimized Fine-Tuning，是一套针对 VLA 微调的优化方案。OpenVLA-OFT 是以预训练 OpenVLA 为基础，应用这套方案得到的策略。

它不是另起炉灶训练的全新视觉语言骨干，也不能简单理解为“原版 OpenVLA 多训练几轮”。OFT 在微调阶段改变了动作解码、动作表示、输出头和训练目标。

标准 OpenVLA-OFT 的四个关键设计是：

1. 并行解码；
2. 动作块；
3. 连续动作表示；
4. L1 回归目标。

### 3.1 并行解码

OFT 在输入序列中放入用于动作预测的占位嵌入，并使用双向注意力，使多个动作位置可以在一次 Transformer 前向计算中产生隐藏表示。

```text
原版 OpenVLA：
输入 → action token 1 → action token 2 → ... → action token D

OpenVLA-OFT：
输入 + 多个动作占位位置
             ↓ 一次前向计算
       所有动作位置的隐藏表示
             ↓ 动作头
          连续动作
```

并行输出不表示各个动作维度彼此独立。它们共享图像、语言和 Transformer 内部表示，模型仍然能够学习各动作维度之间的协调关系。

### 3.2 动作块

Action chunking 指一次预测未来连续多个控制时刻的动作。

如果动作维度 $D=7$，动作块长度 $K=8$，输出可以表示为：

$$
A_t\in\mathbb R^{8\times7}
$$

即一次前向计算生成未来 8 个时刻、每个时刻 7 维的动作，共 56 个连续数值。

```text
获得当前观测
     ↓
预测 [a_t, a_t+1, ..., a_t+7]
     ↓
按顺序执行动作块中的动作
     ↓
获得新观测并重新预测
```

动作块能够提高动作吞吐量，并学习短时间内相互协调的动作序列。它也会降低块内的闭环反馈能力。如果策略完整执行一个长动作块后才重新观察环境，机器人对突发变化的响应会变慢。

例如，机器人以 25 Hz 执行长度为 8 的动作块，整个动作块覆盖：

$$
8/25=0.32\ \text{秒}
$$

如果执行完 8 步后才重新规划，那么这 0.32 秒内不会根据新图像修正策略。实际系统也可以只执行动作块前几步便重新预测，以平衡推理开销和闭环反馈速度。

### 3.3 连续动作头

OFT 使用专门的多层 MLP 动作头，把 Llama 最后一层对应动作位置的隐藏表示直接映射成归一化连续动作：

$$
\hat A_t=f_{\mathrm{head}}(H_t)
$$

论文实现中的 L1 回归动作头是一个带 ReLU 激活的 4 层 MLP。其内部过程可以概括为：

```text
Transformer 隐藏表示
        ↓
Linear → ReLU
        ↓
Linear → ReLU
        ↓
Linear → ReLU
        ↓
最终线性映射
        ↓
归一化连续动作
```

最终输出无需 Softmax，因为机器人动作各维度不表示类别概率，也不需要满足非负且总和为 1。模型输出仍需使用训练数据对应的统计量反归一化，才能得到机器人控制接口所需的单位和范围。

### 3.4 L1 回归目标

标准 OFT 使用预测动作与示范动作之间的平均绝对误差：

$$
\mathcal L_{\mathrm{L1}}
=
\frac{1}{KD}
\sum_{k=0}^{K-1}\sum_{d=1}^{D}
\left|
\hat a_{t+k}^{(d)}-a_{t+k}^{(d)}
\right|
$$

例如，模型预测：

$$
\hat a=[0.12,-0.04,0.31]
$$

示范动作为：

$$
a=[0.10,-0.05,0.28]
$$

对应的未取平均 L1 误差为：

$$
|0.12-0.10|+|-0.04+0.05|+|0.31-0.28|
$$

作者也比较了扩散动作头，但标准 OpenVLA-OFT 采用更简单、推理更快的连续 L1 回归方案。

L1 回归有自身的限制。如果相同观测下存在几种差异很大的合理动作，例如既可以从障碍物左侧绕行，也可以从右侧绕行，确定性 L1 回归可能无法完整表达这种多模态动作分布。论文结果不能推广成“L1 在所有机器人数据上都优于扩散模型”。

### 3.5 更灵活的输入

原版 OpenVLA 的典型配置主要使用单个第三人称相机和语言指令。OFT 的输入管线支持加入：

- 多个摄像头画面，例如第三人称相机和腕部相机；
- 机器人本体状态，例如关节角和夹爪状态；
- 语言指令；
- 多个动作占位位置。

每个图像经过共享的融合视觉编码器和投影器，机器人低维状态则通过单独的投影网络映射到语言模型嵌入空间。随后这些输入沿序列维度拼接，交给 Transformer 统一处理。

## 4. OpenVLA 与 OpenVLA-OFT 的异同

| 对比项 | 原版 OpenVLA | 标准 OpenVLA-OFT |
| --- | --- | --- |
| 基础模型 | Prismatic VLM、Llama 2 7B | 继承 OpenVLA 的预训练骨干和权重 |
| 视觉编码 | DINOv2 + SigLIP | DINOv2 + SigLIP |
| 语言输入 | 支持 | 支持 |
| 典型视觉输入 | 单个第三人称视角 | 可使用多个视角 |
| 机器人状态 | 原始典型配置不使用 | 可通过状态投影器加入 |
| 动作表示 | 每维 256 档离散 token | 连续动作数值 |
| 动作输出头 | 语言模型的线性 LM Head | 专门的多层 MLP 动作头 |
| 解码方式 | 自回归逐 token 解码 | 并行解码 |
| 时间范围 | 通常生成当前一步动作 | 一次生成未来动作块 |
| 训练目标 | 交叉熵 | L1 回归 |
| 动作后处理 | 选 token、反离散化、反归一化 | 反归一化 |
| 主要优势 | 保留统一的语言模型生成形式 | 推理快、动作精细、输入输出灵活 |
| 主要限制 | 顺序生成较慢，存在量化误差 | 长动作块会减少块内闭环反馈；L1 对多模态动作有限制 |

二者的共同点包括：

- 都根据视觉和语言预测机器人动作；
- 都利用 OpenVLA 预训练得到的视觉、语言和机器人操作知识；
- 原始方法主要依赖离线专家示范进行模仿学习；
- 面对新的机器人、摄像头布局、动作接口或任务分布时，通常仍需微调；
- 动作含义、坐标系、归一化统计和底层控制器必须相互匹配。

## 5. OFT 和 LoRA 不是同一个概念

LoRA 和 OFT 解决的问题不同：

| 方法 | 解决的问题 |
| --- | --- |
| LoRA | 如何用较少的可训练参数更新大模型 |
| OFT | 如何设计微调时的动作解码、动作表示、输出形式和损失函数 |

因此，OFT 可以采用 LoRA；原版 OpenVLA 也可以采用 LoRA。官方 OpenVLA-OFT 实验正是使用 LoRA 适配基础模型，同时训练新增的动作头、状态投影器等模块。

## 6. 性能数据应该怎样理解

OpenVLA-OFT 论文在 LIBERO 四组任务上报告了以下平均成功率：

| 方法 | 平均成功率 |
| --- | ---: |
| 使用原始方式微调的 OpenVLA | 76.5% |
| OpenVLA + 并行解码和动作块，仍用离散动作 | 90.2% |
| 连续 L1 方案，单个第三人称视角 | 95.3% |
| 完整 OFT，加入腕部视角和可选机器人状态 | 97.1% |

从 76.5% 到 97.1% 是增加 20.6 个百分点。完整配置同时改变了动作生成方法和输入信息，因此不能把全部提升只归因于 L1 损失。单视角下的 95.3% 更适合用于观察动作表示和训练方法本身的贡献。

论文还报告了约 26 倍的动作生成吞吐提升。理解这一数字时，需要区分：

- 推理延迟：从输入观测到获得一次模型预测所需的时间；
- 动作吞吐量：模型每秒能够生成多少个控制时刻的动作；
- 闭环更新频率：机器人每秒根据新观测重新决策多少次。

动作块会显著提高动作吞吐量，但吞吐提高不等于每秒使用新图像重新规划的次数同等提高。并行解码也没有把 7B 级骨干缩小，所以推理加速不意味着模型参数量或显存占用按相同比例下降。

这些成功率是在 LIBERO 对应任务数据上完成微调后得到的，不表示下载通用权重后在任意机器人上都能直接达到相同成功率。

## 7. OpenVLA-OFT+

OpenVLA-OFT+ 是在 OFT 的基础上加入 FiLM，即 Feature-wise Linear Modulation。它使用语言特征对视觉编码器中的特征进行缩放和平移，使模型更重视语言指定的目标。

例如，场景中同时存在玉米和豆子时，下面两个任务不同指令对应的视觉画面可能几乎相同：

```text
“把玉米舀进碗里”
“把豆子舀进碗里”
```

FiLM 让语言信息更早地参与视觉特征处理，有助于降低策略只依赖场景相关性、忽略语言差异的风险。论文主要在真实双臂 ALOHA 实验中使用 OFT+；标准 OFT 和带 FiLM 的 OFT+ 应当区分。

## 8. 问答一：末端位置变化和姿态变化有什么区别

可以把机械臂末端想象成手里拿着一支笔：

- 位置变化：整支笔从一个地方平移到另一个地方，笔尖的朝向基本不变；
- 姿态变化：笔所在的位置基本不变，但手腕转动，使笔尖朝向发生改变。

对于动作：

$$
[\Delta x,\Delta y,\Delta z,\Delta r_x,\Delta r_y,\Delta r_z]
$$

前三个分量描述平移：

- $\Delta x$：沿 $x$ 轴移动；
- $\Delta y$：沿 $y$ 轴移动；
- $\Delta z$：沿 $z$ 轴移动。

后三个分量描述旋转：

- $\Delta r_x$：绕 $x$ 轴旋转，常类比为 roll；
- $\Delta r_y$：绕 $y$ 轴旋转，常类比为 pitch；
- $\Delta r_z$：绕 $z$ 轴旋转，常类比为 yaw。

以机械臂抓住水杯为例：

```text
位置变化：
整杯水向左移动 2 cm，杯口仍然朝上。

姿态变化：
杯子的位置基本不动，但逐渐倾斜杯子，准备倒水。
```

位置和姿态也可以同时改变。例如，机械臂可以一边把杯子移动到碗上方，一边调整杯口朝向。

具体工程中还要确认：

- 姿态采用欧拉角、旋转向量、四元数还是其他表示；
- 增量是在机器人基座坐标系、相机坐标系还是末端自身坐标系中定义；
- 动作是绝对目标还是相对增量；
- 数值单位是米、弧度，还是经过归一化的无量纲数值。

## 9. 问答二：两者都是通过 MLP 和激活函数得到输出吗

从整个模型看，两者内部都使用 MLP。例如，二者都包含将视觉特征投影到语言模型维度的投影网络。但是在最终动作输出头上，两者有明确区别。

### 9.1 原版 OpenVLA 的输出头

原版 OpenVLA 使用语言模型的 LM Head。它通常是一个线性层，把隐藏表示投影到整个词表的 logits：

$$
h
\xrightarrow{\text{Linear LM Head}}
[z_1,z_2,\ldots,z_V]
$$

其中 $V$ 是词表大小。Softmax 将 logits 转换成类别概率：

$$
p_i=\frac{e^{z_i}}{\sum_j e^{z_j}}
$$

模型随后选择概率最大的 token，或按该分布采样。动作 token 对应一个离散动作区间：

```text
隐藏表示
   ↓ 线性 LM Head
词表 logits
   ↓ Softmax
动作 token 的概率分布
   ↓ 选取 token
离散动作档位
   ↓ 反离散化
近似连续动作
```

Softmax 本身不直接产生机器人动作，它产生的是各 token 的概率。

### 9.2 OpenVLA-OFT 的输出头

OFT 的连续动作头是真正的多层 MLP。中间层通过 ReLU 等非线性激活处理隐藏表示，最终层直接输出连续动作数值：

$$
h
\xrightarrow{\text{4-layer MLP}}
\hat a
$$

它不需要 Softmax，因为动作值可以有正有负，各个维度之间也不需要总和为 1。

### 9.3 离散和连续不只由激活函数决定

一个动作模型最终采用离散输出还是连续输出，主要由以下因素共同决定：

1. 输出空间是词表类别还是实数动作；
2. 训练标签是动作 token 编号还是连续动作值；
3. 使用交叉熵还是 L1 等回归损失；
4. 推理时是选择 token 并反离散化，还是直接反归一化连续值。

因此，最准确的概括是：

```text
原版 OpenVLA：
线性 LM Head → 词表 logits → Softmax/选 token → 离散档位 → 连续动作

OpenVLA-OFT：
多层 MLP 动作头 → 连续归一化动作 → 反归一化 → 连续动作
```

## 10. 对 SO-101 项目的实际意义

将 OpenVLA 或 OpenVLA-OFT 用到 SO-101 时，模型名称并不是首要问题，最先需要确定的是动作接口和控制闭环。

### 10.1 明确动作接口

如果 SO-101 数据记录的是关节目标：

$$
[q_1,q_2,\ldots,q_n,g]
$$

它与末端位姿增量：

$$
[\Delta x,\Delta y,\Delta z,
\Delta r_x,\Delta r_y,\Delta r_z,g]
$$

有完全不同的控制含义。即使两个向量的维度碰巧相同，也不能直接互换。训练标签、动作头维度、归一化统计、控制频率和机器人执行接口必须保持一致。

### 10.2 根据完整链路时延选择动作块

需要测量从相机曝光和采集开始，经过图像传输、预处理、模型推理和控制命令传输，直到舵机开始执行的总时延。动作块太短，可能无法有效分摊大模型推理成本；动作块太长，则会降低根据新观测修正动作的频率。

### 10.3 使用闭环成功率评价策略

较低的离线 L1 误差只说明模型输出接近数据集中的示范动作，不一定代表真实抓取成功。微小动作误差可能导致夹爪错过电池，随后机器人进入训练数据覆盖较少的状态并继续累积误差。

对于 SO-101 电池抓取和摆放任务，最终至少应评估：

- 是否根据指令选择了正确颜色；
- 是否能够稳定抓起并保持电池；
- 是否能够准确放入目标框；
- 遇到位置偏差后能否通过新观测修正；
- 连续执行多个抓放任务时是否发生累积误差。

综合来看，OpenVLA-OFT 的并行解码、连续动作和多输入支持更适合需要较高控制频率的真机适配，但仍需要围绕 SO-101 的动作语义、摄像头配置、控制周期和示范数据完成专门微调与验证。

## 11. 参考资料

- [OpenVLA 论文](https://arxiv.org/abs/2406.09246)
- [OpenVLA 论文 HTML 版](https://arxiv.org/html/2406.09246v3)
- [OpenVLA 官方代码仓库](https://github.com/openvla/openvla)
- [OpenVLA-OFT 论文](https://arxiv.org/abs/2502.19645)
- [OpenVLA-OFT 论文 HTML 版](https://arxiv.org/html/2502.19645v2)
- [OpenVLA-OFT 项目主页](https://openvla-oft.github.io/)
- [OpenVLA-OFT 官方代码仓库](https://github.com/moojink/openvla-oft)
